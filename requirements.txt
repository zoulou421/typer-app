#To install typer, it's very simple, you can use pip:
#pip install typer

#or with virtual environment:
#To update, run: python.exe -m pip install --upgrade pip
#python -m pip install typer

#And if you want to specify the version of Python you're using, for example for Python 3.9:

#On Mac OS

#python3.12 -m pip install typer
#On Windows

#py -3.12 -m pip Install Typer

#on windows if not installed:
#pip install colorama
#typer~=0.12.3
typer~=0.12.3
colorama~=0.4.6
termcolor~=2.4.0